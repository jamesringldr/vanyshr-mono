# Deploy Supabase functions
name: Deploy Functions
on:
  push:
    branches: [main]
    paths:
      - "supabase/functions/**"
      - "packages/backend/**"
      - "workers/**"
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Add deploy steps for packages/backend/functions"
